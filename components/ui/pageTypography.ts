/** Wspólna typografia nagłówków sekcji na stronie głównej (pod hero). */
export const sectionHeadingClass =
  "text-display-md font-extralight leading-[1.12] tracking-[-0.02em]";

export const sectionHeadingInk = "text-zinc-900";
export const sectionHeadingMuted = "text-zinc-400";

/** Mała etykieta nad nagłówkiem (editorial / beauty). */
export const sectionEyebrowClass =
  "mb-4 font-serif text-[0.65rem] font-medium uppercase tracking-[0.22em] text-zinc-500 sm:mb-5";

/** Akapit treści pod nagłówkami — jednolity rytm czytania. */
export const bodyProseClass =
  "text-[0.9375rem] font-light leading-[1.78] text-zinc-600";

/** Rama na zdjęcia sekcji — miękki róg, cień, cienka obwódka. */
export const sectionImageFrame =
  "relative overflow-hidden rounded-2xl bg-zinc-100 shadow-[0_22px_60px_-18px_rgba(15,23,42,0.12)] ring-1 ring-zinc-900/[0.06]";

/** Przycisk CTA (outline → wypełnienie). */
export const ctaButtonClass =
  "inline-flex items-center justify-center rounded-full border border-zinc-900/90 bg-zinc-900 px-8 py-3.5 text-[0.6875rem] font-medium uppercase tracking-[0.18em] text-white shadow-sm transition duration-300 hover:bg-zinc-800 hover:shadow-md active:scale-[0.99]";

/** Drugi wariant CTA — kontur na jasnym tle. */
export const ctaButtonOutlineClass =
  "inline-flex items-center justify-center rounded-full border border-zinc-300 bg-white px-8 py-3.5 text-[0.6875rem] font-medium uppercase tracking-[0.18em] text-zinc-800 transition duration-300 hover:border-zinc-400 hover:bg-zinc-50";
